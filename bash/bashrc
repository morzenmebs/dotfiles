# SSH agent
if [ -z "$SSH_AUTH_SOCK" ]; then
  eval "$(ssh-agent -s)" > /dev/null
  ssh-add -q ~/.ssh/id_ed25519 2>/dev/null
fi

# OpenAI API Key
export OPENAI_API_KEY=$(cat ~/.secrets/openai)

alias mneh='uv run --project ~/mneh mneh'
